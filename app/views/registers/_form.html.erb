<%= form_with(model: register) do |form| %>
  <% if register.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(register.errors.count, "error") %> prohibited this register from being saved:</h2>

      <ul>
        <% register.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
    <div class="activity-card">
      <h2><%= @activity["activity_tittle"] %></h2>
  </div>
    <%= form.number_field :kariah_id, id: :activity_user_id,value:current_kariah.id %>
    <%= form.number_field :kariah_id, id: :activity_user_id,value:current_kariah.email %>
    <%= form.number_field :kariah_id, id: :activity_user_id,value:current_kariah.username %>
    <%= form.number_field :kariah_id, id: :activity_user_id,value:current_kariah.phoneNo %>
    <%= form.number_field :kariah_id, id: :activity_user_id,value:current_kariah.address %>
    <%= form.label :registrationTime, style: "display: block" %>
    <%= form.time_field :registrationTime %>
  </div>

  <div>
    <%= form.label :registrationDate, style: "display: block" %>
    <%= form.date_field :registrationDate %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
